# Uncomment the next line to define a global platform for your project
platform :osx, '10.10'

target 'ControlPanel' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod "PubNub", "~> 4.1"
  pod 'ProgressKit', "~> 0.3"
  pod 'SwiftyJSON'
  #pod 'Material', '2.4.0'
  # Pods for ControlPanel

  target 'ControlPanelTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ControlPanelUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
